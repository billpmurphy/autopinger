Survey the IPv4 world
=====================

This pure-Python tool lets you ping every IPv4 address in a user-defined set of
ranges (up to and including every IPv4 address ever) and records the addresses
of peers that responded.
